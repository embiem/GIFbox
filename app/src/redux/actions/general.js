export const setPhase = phase => ({
  type: 'SET_PHASE',
  phase
});

export const setGIFPath = gifPath => ({
  type: 'SET_GIF_PATH',
  gifPath
});

export const setLastGifPath = lastGifPath => ({
  type: 'SET_LAST_GIF_PATH',
  lastGifPath
});
